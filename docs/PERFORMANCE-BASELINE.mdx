---
title: "Baseline de Performance"
description: "Item #14 do plano — benchmark de percentis (50..500 concorrentes), CPU/RAM e latências de Mongo/Redis pós-correções críticas; como rodar e o que registrar."
slug: "/docs/performance-baseline"
sidebar_label: "Performance Baseline"
---

# Baseline de Performance

## Objetivo

Registrar o comportamento operacional do modelo atual depois das correções críticas
(itens #1–#13): sobre quais níveis de concorrência o sistema ainda é **correto** e onde
a latência/throughput começa a degradar. É medição, não otimização — a fase de
otimização fica para depois deste baseline.

Medidas registradas por onda:

- throughput (ops/s);
- latência por operação: p50, p95, p99, mean, max;
- conflitos (AppError `CONFLICT` 409 do CAS de versão) e retries (reexecuções);
- tempo de transação (duração da onda);
- CPU e RAM do processo da suíte (containers de infra via `docker stats`);
- latência de round-trip de **Mongo** e **Redis** (pings amostrados durante a onda).

## Como rodar

O benchmark roda dentro da rede do `docker-compose.test.yml` (serviço
`integration-tests`), com a infra auto-contida (Mongo replica set `rs0` + Redis) —
o mesmo caminho do CI:

```bash
PERC_LEVELS=50 node scripts/percentile-driver.cjs        # subconjunto (default 50..500)
node scripts/percentile-driver.cjs                        # níveis completos
PERC_LEVELS=50,100,200 PERC_MAX_WAVE_MS=300000 node scripts/percentile-driver.cjs
```

O driver:

1. reconstrói/usa a imagem `backbet:test`;
2. sobe a infra e roda `src/integration/__tests__/load.percentiles.integration.test.ts`;
3. amostra `docker stats` dos containers `mongodb`, `redis` e do próprio runner durante a corrida;
4. grava `scripts/load-results/fase14/<runId>/report.{json,md}` (pasta
   versionada sob `.gitignore`, artefato de medição);
5. faz `down -v` ao final (preservar a infra com `PERC_KEEP_INFRA=1`).

Cenários por nível: **contenção** (N depósitos na *mesma* carteira → serialização de
documento único + CAS/retry) e **distribuído** (N carteiras distintas → eixo horizontal).

## Resultado — base completa pós-correções (22/09/2026)

Run completo: níveis **50/100/200/300/500**, depósitos de R$ 1,25, `runId 302fc891…`,
relatório detalhado em `scripts/load-results/fase14/302fc891-bb01-47da-9e00-9bd0c44a153d/`.

| level | cenário | p50 | p95 | p99 | mean | max | concluídas | rejeitadas | capped | wall_ms | ops/s | cpu% | rss_peak (MB) | mongo ping (ms) | redis ping (ms) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 50 | contenção | 7581 | 10018 | 10042 | 7152 | 10042 | 50 | 0 | não | 10042 | 5 | 80.4 | 597 | 99.5 | 82.5 |
| 50 | distribuído | 348 | 526 | 527 | 406 | 527 | 50 | 0 | não | 527 | 95 | 85.1 | 597 | 143 | 83 |
| 100 | contenção | 16164 | 21192 | 21292 | 14622 | 21298 | 100 | 0 | não | 21298 | 5 | 82.0 | 618 | 120 | 68.5 |
| 100 | distribuído | 657 | 669 | 677 | 621 | 679 | 100 | 0 | não | 679 | 147 | 96.2 | 619 | 184 | 167 |
| 200 | contenção | 68740 | 89952 | 90141 | 60694 | 90181 | 200 | 0 | não | 90181 | 2 | 83.4 | 808 | 195.5 | 179.5 |
| 200 | distribuído | 1525 | 1585 | 1592 | 1509 | 1603 | 200 | 0 | não | 1604 | 125 | 85.1 | 603 | 327.5 | 521 |
| 300 | contenção | 156215 | 204861 | 205249 | 138573 | 205276 | 300 | 0 | não | 205276 | 1 | 82.4 | 1029 | 274 | 183 |
| 300 | distribuído | 1798 | 1876 | 1882 | 1790 | 1883 | 300 | 0 | não | 1884 | 159 | 101.3 | 1012 | 472 | 196.5 |
| 500 | contenção | 449039 | 594989 | 596229 | 399173 | 596312 | 500 | 0 | não | 596313 | 1 | 84.0 | 1024 | 452 | 197 |
| 500 | distribuído | 5724 | 6124 | 6148 | 5730 | 6154 | 500 | 0 | não | 6154 | 81 | 87.2 | 678 | 1557 | 496.5 |

Recursos dos containers (pico durante a corrida):

| container | cpu% pico | cpu% média | RAM pico (MB) | RAM média (MB) |
| --- | --- | --- | --- | --- |
| mongodb | 151.7 | 48.5 | 495.2 | 335.0 |
| redis | 10.2 | 2.3 | 8.2 | 3.5 |
| integration-tests | 171.3 | 38.0 | 1015.0 | 661.0 |

Leitura da rodada completa:

- **Correctness**: **0 rejeitadas, 0 conflitos CAS, 0 ondas capped em TODOS os
  níveis** — o modelo continua correto sob concorrência até 500 ops simultâneas
  (invariantes FI-01/FI-04 etc.). Isso inclui o nível 500 de contenção, que no
  pré-fix rejeitava 174/500.
- **Contenção** degrada **superlinearmente**: p50 ≈ 7,6 s @50 → 16 s @100 → 69 s @200
  → 156 s @300 → **449 s @500** (p95 ≈ 595 s @500; ~1 ops/s). É a serialização do
  documento único por carteira + reexecução de transação otimista — o teto
  patológico confirma-se, e o modelo suporta 500 sem perder invariantes, mas a
  latência é inviável para tráfego real nesse cenário.
- **Distribuído** escala até ~147–159 ops/s (p50 ≈ 0,3–1,8 s) nos níveis 50–300; no
  nível 500 cai para ~81 ops/s com p50 ≈ 5,7 s e ping de Mongo ≈ 1,56 s — sinal de
  saturação de infra (connection pool + host compartilhado), não de bug financeiro.
- **Recursos**: Mongo foi ao pico de 151% CPU com RAM média 335 MB; o runner
  (integration-tests) oscilou até 171% CPU com RSS pico ≈ 1 GB @300/500.
- **Diferenças vs. a rodada de 50x isolada** (21/09): nesta rodada o host também
  rodava a stack de produção (`backbet-*`), elevando a latência base — por isso os
  números @50 ficaram ~20% acima do run isolado. No server01, executar o baseline
  com a stack ativa mede o cenário realista.

### Rodada anterior — 50x isolado (21/09/2026)

Run: nível **50**, `runId 02326fa4…`, relatório em
`scripts/load-results/fase14/02326fa4-7e5e-4185-ac00-475ecf86d9b5/` (sem stack de
produção no host).

| level | cenário | p50 | p95 | p99 | mean | max | concluídas | rejeitadas | wall_ms | ops/s | cpu% | rss_peak (MB) | mongo ping (ms) | redis ping (ms) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 50 | contenção | 6172 | 8047 | 8077 | 5495.7 | 8077 | 50 | 0 | 8077 | 6 | 89.2 | 594.9 | 96 | 53 |
| 50 | distribuído | 713 | 759 | 772 | 713.5 | 772 | 50 | 0 | 772 | 65 | 82.5 | 595.5 | 465 | 3 |

### Referência anterior (pré-fix — 19/09/2026, `scripts/load-results/fase13/final/`)

O baseline antigo já mostrava a mesma degradação superlinear da contenção (p95 ≈
13 s @50 → 449 s @200 → 22 min @300, e 174/500 rejeitadas por WriteConflict @500) e
~97–144 ops/s no distribuído. A correção desta fase **não muda o teto de contenção**
(o gargalo é arquitetural: 1 documento quente por carteira); as recomendações seguem
para a fase de otimização (lock/fila por carteira, saldo agregado, particionamento).

## Comandos de referência

| Comando | O que mede |
|---------|-----------|
| `npm run test:load:percentiles` | Baseline completo (50..500) com telemetria |
| `PERC_LEVELS=50 node scripts/percentile-driver.cjs` | Baseline de um nível |
| `node scripts/merge-perc-reports.cjs` | Mescla runs em um relatório consolidado |