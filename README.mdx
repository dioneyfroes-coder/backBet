---
title: "BackBet"
description: "Backend de apostas esportivas em TypeScript/Node 22+ — visão geral, arquitetura, API v1, operação e contribuição."
slug: "/"
---

# BackBet

Backend de apostas esportivas em **TypeScript + Node 22+** (Express 5), organizado em camadas limpas e DDD. Toda a API pública vive sob o prefixo versionado **`/api/v1`**, o dinheiro é tratado em **centavos inteiros** (sem floats) e a concorrência é protegida por **idempotência** (`Idempotency-Key`) e **locking otimista**.

## O que é e qual problema resolve

BackBet resolve o problema central de um backoffice de apostas: garantir que **fluxos de dinheiro sejam exatos e concorrentes** — depósitos, saques, apostas e tesouraria sem erro de arredondamento e sem saldo "perdido" por corrida de requisições. Para isso, combina:

- **DDD + camadas limpas**: domínio isolado em `src/core/*`, use cases no padrão Result, repositórios trocáveis (in-memory / Mongoose).
- **Dinheiro em centavos**: aritmética inteira (`Money`, `BetAmount`), API sempre em reais.
- **Concorrência defensiva**: `Idempotency-Key` em escritas críticas + `HouseWallet` com `version` e `409 CONFLICT`.
- **Operação simples**: health/readiness, logs JSON, PM2 + workers Bull, Docker Compose replicável.

## Arquitetura

Monólito modular com dependência sempre apontando para dentro:

```
API (Express 5) ▸ Application (use cases) ▸ Domain (entidades/VOs) ▸ Infrastructure ▸ Shared
```

| Contexto | Diretório | Responsabilidades |
|----------|-----------|-------------------|
| User Core | `src/core/user` | Registro, login/JWT, perfil, preferências, recuperação de senha, upload de documentos |
| Finance Core | `src/core/finance` | Carteiras, depósitos, saques, pacotes de crédito, tesouraria (`HouseWallet`) |
| Betting Core | `src/core/betting` | Eventos, odds, colocar/cancelar/resolver apostas |
| Games | `src/core/games` (via `GameController`) | Mini games (Coin Flip: jogar em lote, feed, histórico) |
| Shared Kernel | `src/core/shared` | `AggregateRoot`, `UniqueId`, `Result`, `AppError`, DTOs |

Interfaces (`IUserRepository`, `IWalletRepository`, `IBetRepository`, ...) isolam o domínio; a fábrica em `src/infrastructure/persistence/factory.ts` escolhe o adaptador (in-memory ou Mongo/Mongoose).

## Stack

| Camada | Tecnologia |
|--------|-----------|
| Runtime | Node >= 22, TypeScript 5.9 (`tsx` em dev, `tsc` + `tsc-alias` no build) |
| API | Express 5, Zod (validação de DTOs), helmet, cors, express-rate-limit |
| Auth | Passport + `passport-jwt`, `JwtService` local; bypass em dev (`ALLOW_DEV_BEARER_BYPASS`) |
| Dados | MongoDB/Mongoose (opcional em dev), Redis (`ioredis`, cache + filas Bull) |
| Workers | Bull (contato, saque) — adaptador em memória em dev/test |
| Observabilidade | `/health`, `/readiness`, request-id + logs JSON, OpenTelemetry (OTLP), PM2 WebUI + health-monitor |

## Como executar

1. `cp .env.example .env`
2. Ajuste o essencial: `NODE_ENV=development`, `BACKBET_RUNTIME_ENV=development`, `JWT_SECRET`.
3. Sem infra: `USE_MONGOOSE_PERSISTENCE=false` e `CACHE_ENABLED=false` (in-memory). Para Mongo/Redis reais, defina `MONGODB_URI` e `REDIS_URL`.
4. `npm install`
5. `npm run dev` (hot reload) e teste em `http://localhost:3000/health`.

Ambientes, env vars e troubleshooting: `docs/QUICKSTART.mdx` e `docs/MONGODB_SETUP.mdx`.

## Como testar

| Comando | O que faz |
|---------|-----------|
| `npm run typecheck` | Checagem de tipos (`tsc --noEmit`) |
| `npm run lint` | ESLint em `src/**/*.ts` |
| `npm test` | Suíte unitária Jest (~750 testes) |
| `npm run test:coverage` | Unitária + cobertura |
| `npm run smoke` | `build` + verificação pós-build (`scripts/smoke-api.cjs`) |
| `npm run test:integration` | Suíte real (MongoDB replica set + Redis via `docker-compose.test.yml`) |
| `npm run check` | `check:secrets` + lint + cobertura + build (gate completo) |
| `npm run test:infra:reset` | Sobe infra de teste do zero |

Guia completo: `docs/TESTING-ENV.mdx`; matriz de cenários críticos de dinheiro: `docs/MATRIX-CENARIOS-CRITICOS.mdx`.

## Como usar a API

- **Base URL**: `http://localhost:3000` (dev) / `https://api.backbet.com` (prod). **Todas** as rotas públicas usam `/api/v1`.
- **Envelope**: `{ success, data|error, meta: { requestId, timestamp } }`.
- **Auth**: `Authorization: Bearer <JWT>`; `GET /api/v1/events/*`, `/api/v1/games/*`, `/api/v1/auth/*` e `POST /api/v1/contact` são públicas.
- **Idempotência**: `POST /api/v1/wallets/deposit`, `/api/v1/wallets/withdraw`, `/api/v1/bets`, `/api/v1/bets/:betId/cancel` aceitam `Idempotency-Key` e respondem `Idempotency-Replayed: true` em replay.
- **Paginação**: `?limit=` (max 200, default 50) e `?offset=` (default 0) em listagens.
- **Swagger**: `GET /api/docs` (UI) e `GET /api/docs.json` (spec). Artefato gerado: `docs/openapi.json`.

Contrato completo (códigos de erro, exemplos e fluxos): `docs/API_CONTRACT.mdx`.

## Como subir com Docker

| Compose | Finalidade |
|---------|-----------|
| `docker-compose.yml` | Servidor de aplicação: `backbet` + `redis` + workers (contato/saque) |
| `docker-compose.mongo.yml` | MongoDB (replica set `rs0`, obrigatório p/ transações) no segundo host |
| `docker-compose.test.yml` | Infra de teste isolada (Mongo + Redis no loopback) |
| `docker-compose.proxy.yml` | Reverse proxy (Caddy/TLS) opcional |

Produção: `docker-compose.mongo.yml` (server02) → `MONGODB_URI` no `.env` → `docker compose up -d --build` (server01). Guias: `docs/DOCKER.mdx`, `docs/DEPLOY.mdx`.

## Como o dinheiro funciona

- **Centavos**: todo valor interno é inteiro em centavos (`Money`, `BetAmount`); a API expõe reais. Nada de `float` em fluxo de valor.
- **Carteira**: saldo por usuário (`Wallet`) movimentado via transações financeiras; cache Redis invalidado após mutações.
- **Tesouraria**: `HouseWallet` concentra ganhos/perdas do operador e é o alvo dos settlements.
- **Saques/depósitos**: PIX via `PIX_PROVIDER=mock` (sem provedor externo) com fila `Bull` para saques.
- **Rate limits**: globais + específicos (`AUTH_*_RATE_LIMIT_*`, `WALLET_*_RATE_LIMIT_*`, `BET_*_RATE_LIMIT_*`).

Detalhes do módulo e casos de borda: `docs/Finance_docs.mdx` e `docs/MATRIX-CENARIOS-CRITICOS.mdx`.

## Como a concorrência funciona

- **Idempotência**: escritas críticas reservam a chave no Redis (`SET NX`); replay da mesma chave retorna o mesmo resultado (`Idempotency-Replayed: true`), payload diferente → `409`, reserva em andamento → `409`.
- **Locking otimista**: `HouseWallet`/carteiras usam `version`; escrita com versão obsoleta retorna `409 CONFLICT` (retry do cliente).
- **Filas**: operações assíncronas (contato, saque) vão para Bull; falhas são registradas e processadas posteriormente.
- **Persistência transacional**: Mongo como replica set habilita transações (wallet/ledger/bet/treasury) atômicas.

## Como contribuir

- Commits convencionais (`feat:`, `fix:`, `docs:`, ...) e gates verdes em PR: `npm run check` + `npm run smoke`.
- Novos módulos seguem `src/core/<domínio>/application/use-cases`; docs e código mudam juntos.
- Veja `docs/CONTRIBUTING.mdx` para o checklist completo.

## Status do projeto

- **v1.0.0 consolidada** (ciclo MVP encerrado em 30/08/2026): API versionada `/api/v1` (Fase 30) com contrato, envelope padronizado, paginação, idempotência e OpenAPI; suíte unitária com 784 testes, gate final `npm run check` verde e invariantes financeiros FI-01..FI-10 testados.
- **Definition of Done (seção 41 do plano)**: 48/50 itens atendidos com evidência (`docs/DEFINITION_OF_DONE.mdx`); os 2 restantes exigem ambiente com Docker/CI. Aceite final do MVP: `docs/GO_NOGO_FINAL.mdx`.
- **Requisitos operacionais cobertos**: auditoria/retention, transmissão SIGAP/SPA, Docker/deploy, backup/recuperação, segredos e endurecimento de segurança; auto-avaliação executável via `npm run security:assess` (`docs/CERTIFICACAO_PREPARACAO.mdx`).
- **Pendências/roadmap**: build da imagem Docker e integração real (verificação em ambiente com Docker/CI), provedores reais W-01..W-05 (KYC/geolocalização/SIGAP/PSP) e certificação SPA — dependem de operador autorizado (projeto separado de compliance).

## Documentação relacionada

**Produto/arquitetura**: `docs/ARCHITECTURE.mdx` · `docs/User_docs.mdx` · `docs/Finance_docs.mdx` · `docs/Bet_docs.mdx` · `docs/GAMES_AUTH.mdx` · `docs/risk-integration.mdx`

**API**: `docs/API_CONTRACT.mdx` (contrato v1) · `docs/openapi.json` (OpenAPI 3.1)

**Dados & identidade**: `docs/MONGODB_SETUP.mdx` · `docs/MONGOOSE_REPOSITORIES.mdx` · `docs/PASSPORT_SETUP.mdx`

**Observabilidade**: `docs/OBSERVABILITY.mdx` · `docs/LOGS.mdx` · `docs/PM2.mdx` · `docs/RESILIENCE.mdx`

**Deploy & operação**: `docs/DEPLOY.mdx` · `docs/DOCKER.mdx` · `docs/BACKUP_RECOVERY.mdx` · `docs/SECRETS.mdx` · `docs/SECURITY-OPS.mdx` · `docs/AUDIT_RETENTION.mdx` · `docs/SIGAP_CONFORMIDADE.mdx`

**Testes & QA**: `docs/TESTING-ENV.mdx` · `docs/MATRIX-CENARIOS-CRITICOS.mdx`

**Governança**: `docs/CONTRIBUTING.mdx` · `docs/CONTRIBUTORS.mdx` · `docs/LICENSE.mdx`

**Conclusão do ciclo MVP**: `docs/DEFINITION_OF_DONE.mdx` (DoD, seção 41) · `docs/GO_NOGO_FINAL.mdx` (aceite final, seção 44) · `docs/COMERCIAL_ESTRATEGIA.mdx` (decisão de produto, Fase 36)

**Guia rápido**: `docs/QUICKSTART.mdx`

**Histórico**: `docs/archive/CODE_REVIEW_DIAGNOSTIC.mdx` (snapshot de revisão, nov/2025)

**Versão atual:** 1.0.0 · Doc principal reescrito em 29 de agosto de 2026.