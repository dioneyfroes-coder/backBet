schema: v1
name: ollama-mcp
version: 1.0.0

mcpServers:
  - name: ollama
    command: npx
    args:
      - -y
      - github:modelcontextprotocol/servers/ollama
    stdio: true
    env:
      OLLAMA_HOST: "http://localhost:11434"
